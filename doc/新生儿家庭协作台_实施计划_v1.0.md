# 新生儿家庭协作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 40 小时内交付一个移动端优先的 Web MVP，跑通“视频理解 → 家庭画像 → 协作方案 → 任务执行 → 动态调整”的真实 AI 闭环。

**Architecture:** 单仓库全栈 TypeScript：React/Vite 负责移动端界面，Express 负责文件上传、临时媒体访问、豆包模型代理和结构校验。前后端共享 Zod 数据契约；浏览器用持久化状态保存临时家庭空间，服务端只保存 24 小时内的媒体和必要请求状态。

**Tech Stack:** React、TypeScript、Vite、Express、Zod、Zustand、Vitest、Testing Library、Supertest、Playwright、Multer、火山方舟 OpenAI 兼容 API

---

## 0. 执行约束

- 需求基线：`docs/新生儿家庭协作台_PRD_v1.0.md`。
- 当前 `app` 与 `design` 目录为空，当前工作区不是 Git 仓库。
- 开始编码时先初始化 Git；不要把 `.env`、上传视频或运行日志提交到仓库。
- 每项功能遵循红—绿—重构：先写失败测试，再实现最小逻辑。
- 一个核心任务对应一次提交；提交前运行该任务列出的验证命令。
- P0 未全部通过前，不实现复盘、多人独立登录、通知、日历和小程序。
- 所有模型返回必须通过 Zod 校验，不能把未校验文本直接渲染成正式方案。
- `ARK_API_KEY` 只允许在服务端读取。

## 1. 文件结构

```text
大区赛/
├── app/
│   ├── package.json                 # 脚本与依赖
│   ├── .env.example                 # 环境变量名称，不含密钥
│   ├── .gitignore                   # 忽略密钥、构建物、媒体与日志
│   ├── index.html                   # Vite 入口
│   ├── vite.config.ts               # React、测试与开发代理
│   ├── tsconfig.json                # 浏览器端 TypeScript
│   ├── tsconfig.server.json         # 服务端 TypeScript
│   ├── playwright.config.ts         # 移动端 E2E 配置
│   ├── src/
│   │   ├── main.tsx                 # React 挂载
│   │   ├── App.tsx                  # 路由与全局外壳
│   │   ├── styles/
│   │   │   ├── tokens.css           # 从 Figma 同步的令牌
│   │   │   └── global.css           # 全局与移动端样式
│   │   ├── domain/
│   │   │   ├── schemas.ts           # 前后端共享 Zod 契约
│   │   │   ├── types.ts             # 从 Schema 推导的类型
│   │   │   └── rules.ts             # 安全与分配确定性规则
│   │   ├── state/
│   │   │   └── family-store.ts      # 临时家庭空间与持久化
│   │   ├── api/
│   │   │   └── client.ts            # 上传、解析、规划、调整 API
│   │   ├── pages/
│   │   │   ├── HomePage.tsx         # 示例与上传入口
│   │   │   ├── ContentReviewPage.tsx# 解析结果确认
│   │   │   ├── FamilyPage.tsx       # 家庭画像
│   │   │   ├── PlanPage.tsx         # 协作方案确认
│   │   │   └── TodayPage.tsx        # 今日执行台
│   │   └── components/
│   │       ├── AppShell.tsx          # 顶栏与底部导航
│   │       ├── AsyncState.tsx        # 加载/失败/重试状态
│   │       ├── VideoSourcePicker.tsx # 示例与真实上传
│   │       ├── StepEditor.tsx        # 护理步骤编辑
│   │       ├── MemberEditor.tsx      # 成员画像编辑
│   │       ├── TaskCard.tsx          # 任务卡与状态操作
│   │       └── AdjustmentSheet.tsx   # 动态调整前后对比
│   ├── server/
│   │   ├── index.ts                  # HTTP 启动与定时清理
│   │   ├── app.ts                    # Express 装配
│   │   ├── config.ts                 # 环境变量校验
│   │   ├── prompts.ts                # 三类模型 Prompt
│   │   ├── routes/
│   │   │   ├── media.ts              # 上传与临时读取
│   │   │   ├── analyze.ts            # 视频理解
│   │   │   ├── plan.ts               # 协作方案生成
│   │   │   └── adjust.ts             # 局部调整
│   │   └── services/
│   │       ├── ark-client.ts          # 火山方舟适配器
│   │       ├── media-store.ts         # 24 小时临时媒体
│   │       ├── model-json.ts          # JSON 提取、校验、单次修复
│   │       └── safety.ts              # 高风险内容拦截
│   └── tests/
│       ├── fixtures/
│       │   ├── analysis.json          # 合法视频解析结果
│       │   ├── plan.json              # 合法协作方案
│       │   └── adjustment.json        # 合法调整结果
│       ├── unit/
│       │   ├── schemas.test.ts
│       │   ├── rules.test.ts
│       │   ├── model-json.test.ts
│       │   └── media-store.test.ts
│       ├── api/
│       │   ├── media.test.ts
│       │   ├── analyze.test.ts
│       │   ├── plan.test.ts
│       │   └── adjust.test.ts
│       ├── components/
│       │   ├── VideoSourcePicker.test.tsx
│       │   ├── MemberEditor.test.tsx
│       │   └── TaskCard.test.tsx
│       └── e2e/
│           ├── preset-flow.spec.ts
│           └── adjustment-flow.spec.ts
└── design/
    ├── README.md                     # Figma 链接、页面状态和导出规则
    ├── tokens.json                   # 品牌色、间距、圆角、字体
    └── figma-handoff.md              # 页面清单、组件变体、验收尺寸
```

## 2. 四人并行分工

| 负责人 | 主责 | 首个依赖完成后可并行的任务 |
|---|---|---|
| A：产品/设计 | Figma、令牌、页面状态、演示叙事 | Task 1、Task 5、Task 10 |
| B：前端 | 应用外壳、上传/确认、画像、今日执行台 | Task 2 后执行 Task 5、6、8 |
| C：服务端 | 文件上传、媒体访问、模型代理 | Task 2 后执行 Task 3、4 |
| D：Agent/联调 | Prompt、Schema、规划与调整、测试样本 | Task 2 后执行 Task 4、7、9 |

共享文件规则：

- `src/domain/schemas.ts` 由 D 维护，修改前同步 B/C。
- `src/styles/tokens.css` 由 A 维护，其他人只消费变量。
- `server/app.ts` 由 C 维护，其他服务端任务通过独立 router 接入。
- 每 4 小时执行一次主分支集成，禁止最后 2 小时集中合并。

---

### Task 1: 锁定 Figma 基线与验收状态

**Files:**
- Create: `design/README.md`
- Create: `design/tokens.json`
- Create: `design/figma-handoff.md`
- Create: `app/src/styles/tokens.css`

- [ ] **Step 1: 在 Figma 建立 390×844 主画板**

建立 5 个主页面：首页、内容确认、家庭画像、方案确认、今日协作台。每页至少包含 default、loading、empty、error 四种状态；今日协作台额外包含 adjustment 状态。

- [ ] **Step 2: 写入固定设计令牌**

`design/tokens.json`：

```json
{
  "color": {
    "brand": "#6E7CF6",
    "brandSoft": "#EEF0FF",
    "text": "#22252B",
    "muted": "#69707D",
    "surface": "#FFFFFF",
    "background": "#F7F8FC",
    "success": "#2B9A66",
    "warning": "#C47B16",
    "danger": "#C9414B",
    "border": "#E4E7EE"
  },
  "radius": { "small": 8, "medium": 14, "large": 20 },
  "space": { "xs": 4, "sm": 8, "md": 16, "lg": 24, "xl": 32 },
  "font": { "body": 16, "caption": 14, "title": 24 }
}
```

- [ ] **Step 3: 同步 CSS 变量**

`app/src/styles/tokens.css`：

```css
:root {
  --color-brand: #6e7cf6;
  --color-brand-soft: #eef0ff;
  --color-text: #22252b;
  --color-muted: #69707d;
  --color-surface: #ffffff;
  --color-background: #f7f8fc;
  --color-success: #2b9a66;
  --color-warning: #c47b16;
  --color-danger: #c9414b;
  --color-border: #e4e7ee;
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
}
```

- [ ] **Step 4: 写交付说明并人工验收**

`design/figma-handoff.md` 必须列出 5 个页面、TaskCard 的 7 个状态、按钮的 4 个状态、底部抽屉和 375/390/430 px 三种宽度。逐页与 PRD 第 5、6、9 节核对。

- [ ] **Step 5: 提交设计基线**

```powershell
git add design app/src/styles/tokens.css
git commit -m "design: define mobile collaboration design system"
```

---

### Task 2: 初始化工程并建立共享数据契约

**Files:**
- Create: `app/package.json`
- Create: `app/.env.example`
- Create: `app/.gitignore`
- Create: `app/vite.config.ts`
- Create: `app/tsconfig.json`
- Create: `app/tsconfig.server.json`
- Create: `app/src/domain/schemas.ts`
- Create: `app/src/domain/types.ts`
- Test: `app/tests/unit/schemas.test.ts`

- [ ] **Step 1: 初始化仓库与依赖**

Run:

```powershell
git init
Set-Location app
npm init -y
npm install react react-dom react-router-dom express multer zod zustand
npm install -D typescript vite @vitejs/plugin-react vitest jsdom @testing-library/react @testing-library/jest-dom @types/react @types/react-dom @types/express @types/multer tsx supertest @types/supertest concurrently playwright @playwright/test
```

Expected: 依赖安装成功，`npm audit` 的结果被记录但不阻塞脚手架；高危运行时漏洞必须在 Task 11 前清零。

- [ ] **Step 2: 写失败的 Schema 测试**

`app/tests/unit/schemas.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { analysisSchema, memberSchema, taskSchema } from '../../src/domain/schemas';

describe('domain schemas', () => {
  it('rejects medical steps as executable tasks', () => {
    expect(() => taskSchema.parse({
      id: 'task-1', title: '服用药物', sourceStepId: 'step-1',
      assigneeId: 'member-1', collaboratorIds: [], timeSlot: 'night',
      durationMin: 10, assignmentReason: '按视频执行', status: 'pending',
      lockedByUser: false, riskLevel: 'medical', version: 1
    })).toThrow();
  });

  it('accepts a confirmed low-risk analysis', () => {
    const value = analysisSchema.parse({
      videoId: 'video-1', topic: '睡前用品准备', applicableScene: '睡前护理',
      supplies: ['干净衣物'], cautions: ['保持用品清洁'], uncertainties: [],
      steps: [{ id: 'step-1', order: 1, title: '准备用品', instruction: '将用品放在伸手可及处',
        startSec: 10, endSec: 25, supplies: ['干净衣物'], caution: '', riskLevel: 'low', userConfirmed: true }]
    });
    expect(value.steps).toHaveLength(1);
  });

  it('rejects members without a display name', () => {
    expect(() => memberSchema.parse({ id: 'm1', displayName: '', role: '爸爸', experience: 'beginner',
      availableSlots: ['night'], limitations: [], preference: 'assist', temporaryUnavailable: false })).toThrow();
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

Run: `npm test -- --run tests/unit/schemas.test.ts`  
Expected: FAIL，提示无法找到 `src/domain/schemas`。

- [ ] **Step 4: 实现最小共享 Schema**

`app/src/domain/schemas.ts`：

```ts
import { z } from 'zod';

export const riskSchema = z.enum(['low', 'confirm', 'medical']);
export const timeSlotSchema = z.enum(['morning', 'daytime', 'evening', 'night']);
export const stepSchema = z.object({
  id: z.string().min(1), order: z.number().int().min(1).max(10),
  title: z.string().min(1).max(50), instruction: z.string().min(1).max(500),
  startSec: z.number().int().min(0).nullable(), endSec: z.number().int().min(0).nullable(),
  supplies: z.array(z.string().min(1).max(30)).max(10), caution: z.string().max(300),
  riskLevel: riskSchema, userConfirmed: z.boolean()
}).refine(v => v.startSec === null || v.endSec === null || v.endSec >= v.startSec, 'endSec must follow startSec');

export const analysisSchema = z.object({
  videoId: z.string().min(1), topic: z.string().min(1).max(100),
  applicableScene: z.string().min(1).max(100), supplies: z.array(z.string()).max(20),
  cautions: z.array(z.string()).max(20), uncertainties: z.array(z.string()).max(20),
  steps: z.array(stepSchema).min(1).max(10)
});

export const memberSchema = z.object({
  id: z.string().min(1), displayName: z.string().trim().min(1).max(20), role: z.string().min(1).max(20),
  experience: z.enum(['beginner', 'basic', 'experienced', 'professional']),
  availableSlots: z.array(timeSlotSchema).max(4), limitations: z.array(z.string().max(100)).max(10),
  preference: z.enum(['lead', 'assist', 'simple']), temporaryUnavailable: z.boolean()
});

export const taskSchema = z.object({
  id: z.string().min(1), title: z.string().min(1).max(50), sourceStepId: z.string().min(1),
  assigneeId: z.string().nullable(), collaboratorIds: z.array(z.string()).max(3), timeSlot: timeSlotSchema.nullable(),
  durationMin: z.number().int().min(1).max(120), assignmentReason: z.string().min(1).max(150),
  status: z.enum(['pending', 'upcoming', 'in_progress', 'completed', 'skipped', 'affected', 'cancelled']),
  lockedByUser: z.boolean(), riskLevel: z.enum(['low', 'confirm']), version: z.number().int().min(1)
});

export const planSchema = z.object({ id: z.string().min(1), status: z.enum(['draft', 'confirmed']), tasks: z.array(taskSchema).max(20) });
```

`app/src/domain/types.ts`：

```ts
import type { z } from 'zod';
import { analysisSchema, memberSchema, planSchema, stepSchema, taskSchema } from './schemas';
export type Analysis = z.infer<typeof analysisSchema>;
export type CareStep = z.infer<typeof stepSchema>;
export type FamilyMember = z.infer<typeof memberSchema>;
export type CollaborationTask = z.infer<typeof taskSchema>;
export type CollaborationPlan = z.infer<typeof planSchema>;
```

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/unit/schemas.test.ts`  
Expected: 3 tests PASS。

```powershell
git add app
git commit -m "feat: establish full-stack contracts"
```

---

### Task 3: 实现临时媒体上传、校验与 24 小时清理

**Files:**
- Create: `app/server/config.ts`
- Create: `app/server/services/media-store.ts`
- Create: `app/server/routes/media.ts`
- Create: `app/server/app.ts`
- Create: `app/server/index.ts`
- Test: `app/tests/unit/media-store.test.ts`
- Test: `app/tests/api/media.test.ts`

- [ ] **Step 1: 写上传失败测试**

`app/tests/api/media.test.ts` 核心断言：

```ts
import request from 'supertest';
import { describe, expect, it } from 'vitest';
import { createApp } from '../../server/app';

describe('POST /api/media', () => {
  it('rejects unsupported files', async () => {
    const response = await request(createApp()).post('/api/media').attach('video', Buffer.from('plain text'), 'note.txt');
    expect(response.status).toBe(415);
    expect(response.body.code).toBe('UNSUPPORTED_VIDEO');
  });

  it('accepts an mp4 and returns a media URL', async () => {
    const response = await request(createApp()).post('/api/media').attach('video', Buffer.from('fake-mp4'), {
      filename: 'care.mp4', contentType: 'video/mp4'
    });
    expect(response.status).toBe(201);
    expect(response.body.mediaUrl).toMatch(/\/api\/media\//);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/api/media.test.ts`  
Expected: FAIL，无法找到 `server/app`。

- [ ] **Step 3: 实现媒体存储接口**

`media-store.ts` 暴露以下固定接口：

```ts
export interface StoredMedia { id: string; path: string; mimeType: string; expiresAt: number }
export interface MediaStore {
  save(buffer: Buffer, mimeType: string): Promise<StoredMedia>;
  find(id: string): Promise<StoredMedia | null>;
  removeExpired(now?: number): Promise<number>;
}
```

使用 `crypto.randomUUID()` 命名文件；只允许 `video/mp4`、`video/quicktime`、`video/webm`；Multer `limits.fileSize` 设置为 `100 * 1024 * 1024`。媒体读取路由按记录中的 MIME 返回文件，不接受用户提供的磁盘路径。

- [ ] **Step 4: 加入统一错误响应**

所有媒体错误使用：

```ts
type ApiError = { code: 'VIDEO_REQUIRED' | 'UNSUPPORTED_VIDEO' | 'VIDEO_TOO_LARGE' | 'MEDIA_NOT_FOUND'; message: string };
```

服务启动后每 30 分钟执行一次 `removeExpired()`；测试通过注入临时目录，禁止读写真实 `server/uploads`。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/unit/media-store.test.ts tests/api/media.test.ts`  
Expected: 上传格式、读取、过期删除测试全部 PASS。

```powershell
git add app/server app/tests
git commit -m "feat: add expiring video upload service"
```

---

### Task 4: 接入豆包视频理解并保证结构化输出

**Files:**
- Create: `app/server/prompts.ts`
- Create: `app/server/services/ark-client.ts`
- Create: `app/server/services/model-json.ts`
- Create: `app/server/services/safety.ts`
- Create: `app/server/routes/analyze.ts`
- Create: `app/tests/fixtures/analysis.json`
- Test: `app/tests/unit/model-json.test.ts`
- Test: `app/tests/api/analyze.test.ts`

- [ ] **Step 1: 写 JSON 提取与风险拦截失败测试**

```ts
import { describe, expect, it } from 'vitest';
import { parseAnalysisOutput } from '../../server/services/model-json';

describe('parseAnalysisOutput', () => {
  it('parses fenced JSON', () => {
    const raw = '```json\n{"videoId":"v1","topic":"用品准备","applicableScene":"睡前",' +
      '"supplies":[],"cautions":[],"uncertainties":[],"steps":[{"id":"s1","order":1,' +
      '"title":"整理用品","instruction":"将衣物放好","startSec":1,"endSec":8,"supplies":[],' +
      '"caution":"","riskLevel":"low","userConfirmed":false}]}\n```';
    expect(parseAnalysisOutput(raw, 'v1').topic).toBe('用品准备');
  });

  it('forces medicine instructions to medical risk', () => {
    const raw = '{"videoId":"v1","topic":"护理","applicableScene":"日常","supplies":[],' +
      '"cautions":[],"uncertainties":[],"steps":[{"id":"s1","order":1,"title":"服用药物",' +
      '"instruction":"按视频剂量喂药","startSec":1,"endSec":8,"supplies":[],"caution":"",' +
      '"riskLevel":"low","userConfirmed":false}]}';
    expect(parseAnalysisOutput(raw, 'v1').steps[0].riskLevel).toBe('medical');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/unit/model-json.test.ts`  
Expected: FAIL，无法找到 `parseAnalysisOutput`。

- [ ] **Step 3: 实现模型适配器**

`ark-client.ts` 使用服务端 `fetch` 调用 `https://ark.cn-beijing.volces.com/api/v3/chat/completions`，模型固定为 `doubao-seed-2-0-lite-260428`，消息内容固定包含：

```ts
const content = [
  { type: 'video_url', video_url: { url: mediaUrl } },
  { type: 'text', text: prompt }
];
```

请求头使用 `Authorization: Bearer ${ARK_API_KEY}`；使用 `AbortSignal.timeout(90000)`；非 2xx 响应映射为 `ARK_UNAVAILABLE`，超时映射为 `ARK_TIMEOUT`。官方文档确认该模型支持视频、音频、图像和文本的统一理解；开发首日必须用控制台同模型示例核对请求字段。

- [ ] **Step 4: 实现固定 Prompt、解析和单次修复**

视频理解 Prompt 必须要求仅输出 JSON，字段与 `analysisSchema` 一致；明确：最多 10 步、必须提供时间点、不确定内容进入 `uncertainties`、医疗内容使用 `medical`。

`parseAnalysisOutput()`：去除代码围栏 → `JSON.parse` → 覆盖 `videoId` → 执行关键词风险升级 → `analysisSchema.parse`。首次校验失败时，使用原始输出和 Zod 错误调用一次修复 Prompt；第二次失败返回 `INVALID_MODEL_OUTPUT`。

- [ ] **Step 5: API 测试、真实冒烟测试与提交**

单元/API 测试使用注入的假 `ArkClient`，不消耗额度：

Run: `npm test -- --run tests/unit/model-json.test.ts tests/api/analyze.test.ts`  
Expected: 围栏 JSON、非法 JSON、单次修复、医疗升级、超时映射全部 PASS。

真实 Key 冒烟测试：

```powershell
if ([string]::IsNullOrWhiteSpace($env:ARK_API_KEY)) { throw '请先在当前终端设置 ARK_API_KEY' }
if ([string]::IsNullOrWhiteSpace($env:PRESET_VIDEO_URL)) { throw '请先在当前终端设置 PRESET_VIDEO_URL' }
npm run smoke:ark -- --media-url $env:PRESET_VIDEO_URL
```

Expected: 输出通过 `analysisSchema` 的 JSON；终端与日志不打印 Key。

```powershell
git add app/server app/tests
git commit -m "feat: analyze care videos with Doubao"
```

---

### Task 5: 完成首页、上传进度和内容确认页

**Files:**
- Create: `app/src/main.tsx`
- Create: `app/src/App.tsx`
- Create: `app/src/styles/global.css`
- Create: `app/src/api/client.ts`
- Create: `app/src/components/AppShell.tsx`
- Create: `app/src/components/AsyncState.tsx`
- Create: `app/src/components/VideoSourcePicker.tsx`
- Create: `app/src/components/StepEditor.tsx`
- Create: `app/src/pages/HomePage.tsx`
- Create: `app/src/pages/ContentReviewPage.tsx`
- Test: `app/tests/components/VideoSourcePicker.test.tsx`

- [ ] **Step 1: 写文件校验失败测试**

```tsx
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';
import { VideoSourcePicker } from '../../src/components/VideoSourcePicker';

describe('VideoSourcePicker', () => {
  it('explains unsupported formats', () => {
    render(<VideoSourcePicker onSelected={vi.fn()} />);
    const input = screen.getByLabelText('上传护理视频');
    fireEvent.change(input, { target: { files: [new File(['x'], 'care.txt', { type: 'text/plain' })] } });
    expect(screen.getByText('支持 MP4、MOV 或 WebM 视频')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/components/VideoSourcePicker.test.tsx`  
Expected: FAIL，组件不存在。

- [ ] **Step 3: 实现双入口与状态机**

首页固定提供“体验示例视频”和“上传护理视频”。真实上传状态使用联合类型：

```ts
type UploadState =
  | { kind: 'idle' }
  | { kind: 'uploading'; progress: number }
  | { kind: 'analyzing'; stage: 'understanding' | 'structuring' | 'checking' }
  | { kind: 'success'; videoId: string }
  | { kind: 'error'; message: string; retryable: boolean };
```

上传使用 `XMLHttpRequest` 获取真实进度；解析阶段按服务端请求状态显示文案。错误态必须保留“重试”“换一个视频”“体验示例”三个恢复动作。

- [ ] **Step 4: 实现可编辑步骤确认**

`StepEditor` 支持修改标题、说明、时间点、注意事项和删除；`medical` 步骤显示红色风险块且不可勾选为可执行。只有所有非医疗步骤 `userConfirmed=true` 后，“建立家庭画像”按钮才可用。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/components/VideoSourcePicker.test.tsx`  
Expected: 格式错误、上传中禁用、错误恢复、示例入口测试全部 PASS。

Run: `npm run build`  
Expected: TypeScript 与 Vite 构建成功。

```powershell
git add app/src app/tests/components
git commit -m "feat: add video intake and content review"
```

---

### Task 6: 实现家庭画像与本地持久化

**Files:**
- Create: `app/src/state/family-store.ts`
- Create: `app/src/components/MemberEditor.tsx`
- Create: `app/src/pages/FamilyPage.tsx`
- Test: `app/tests/components/MemberEditor.test.tsx`
- Test: `app/tests/unit/rules.test.ts`

- [ ] **Step 1: 写模板与约束测试**

```tsx
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { MemberEditor } from '../../src/components/MemberEditor';

describe('MemberEditor', () => {
  it('creates the three-member quick template', () => {
    render(<MemberEditor />);
    fireEvent.click(screen.getByRole('button', { name: '快速创建家庭' }));
    expect(screen.getByText('妈妈')).toBeInTheDocument();
    expect(screen.getByText('爸爸')).toBeInTheDocument();
    expect(screen.getByText('奶奶')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/components/MemberEditor.test.tsx`  
Expected: FAIL，组件不存在。

- [ ] **Step 3: 实现 Zustand 家庭空间**

Store 固定保存：`spaceId`、`analysis`、`members`、`plan`、`adjustmentHistory`。使用 `persist` 中间件，存储键为 `family-collaboration-space-v1`。快速模板创建妈妈、爸爸、奶奶；成员 ID 使用 `crypto.randomUUID()`。

- [ ] **Step 4: 实现画像表单和冲突提示**

成员最少 1 人、最多 8 人；删除有未完成任务的成员时先显示任务数量，再允许“让 Agent 重新安排”或取消。所有成员不可用时显示“当前没有可分配成员”，不阻止保存，但禁用直接确认方案。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/components/MemberEditor.test.tsx tests/unit/rules.test.ts`  
Expected: 快速模板、必填校验、8 人上限、不可用成员过滤全部 PASS。

```powershell
git add app/src/state app/src/components app/src/pages app/tests
git commit -m "feat: capture family availability and constraints"
```

---

### Task 7: 生成可解释且受约束的协作方案

**Files:**
- Modify: `app/server/prompts.ts`
- Create: `app/src/domain/rules.ts`
- Create: `app/server/routes/plan.ts`
- Create: `app/tests/fixtures/plan.json`
- Test: `app/tests/unit/rules.test.ts`
- Test: `app/tests/api/plan.test.ts`

- [ ] **Step 1: 写确定性安全规则测试**

```ts
import { describe, expect, it } from 'vitest';
import { validateAssignments } from '../../src/domain/rules';

describe('validateAssignments', () => {
  it('rejects night work for a limited member', () => {
    const members = [{ id: 'dad', displayName: '爸爸', role: '爸爸', experience: 'basic' as const,
      availableSlots: ['night' as const], limitations: ['不可夜间照护'], preference: 'assist' as const, temporaryUnavailable: false }];
    const tasks = [{ id: 't1', title: '夜间准备', sourceStepId: 's1', assigneeId: 'dad', collaboratorIds: [],
      timeSlot: 'night' as const, durationMin: 10, assignmentReason: '晚间可用', status: 'pending' as const,
      lockedByUser: false, riskLevel: 'low' as const, version: 1 }];
    expect(validateAssignments(tasks, members)).toContainEqual(expect.objectContaining({ taskId: 't1' }));
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/unit/rules.test.ts tests/api/plan.test.ts`  
Expected: FAIL，规则函数和路由不存在。

- [ ] **Step 3: 实现规划 Prompt 与后置规则**

Prompt 依次声明：安全限制优先、可用时间、经验、偏好、负载均衡、每项任务说明理由、无人符合时 `assigneeId=null`。模型输出通过 `planSchema` 后，再执行 `validateAssignments()`；违反硬限制的任务清空负责人并标记冲突，不自动接受模型分配。

- [ ] **Step 4: 实现 `/api/plan`**

请求体为 `{ analysis, members, lockedTasks }`；响应为 `{ plan, conflicts }`。仅允许 `analysis` 中已确认且非 `medical` 的步骤进入规划。模型引用不存在成员、遗漏来源步骤或生成超过 20 项任务时触发一次修复；仍失败返回 `INVALID_PLAN`。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/unit/rules.test.ts tests/api/plan.test.ts`  
Expected: 夜间限制、临时不可用、未知成员、医疗步骤排除、锁定任务保留全部 PASS。

```powershell
git add app/src/domain app/server app/tests
git commit -m "feat: generate constrained family plans"
```

---

### Task 8: 实现方案确认和今日协作台

**Files:**
- Create: `app/src/pages/PlanPage.tsx`
- Create: `app/src/pages/TodayPage.tsx`
- Create: `app/src/components/TaskCard.tsx`
- Modify: `app/src/state/family-store.ts`
- Test: `app/tests/components/TaskCard.test.tsx`

- [ ] **Step 1: 写任务状态测试**

```tsx
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';
import { TaskCard } from '../../src/components/TaskCard';

describe('TaskCard', () => {
  it('completes once and offers undo', () => {
    const onComplete = vi.fn();
    render(<TaskCard task={{ id: 't1', title: '准备衣物', sourceStepId: 's1', assigneeId: 'dad',
      collaboratorIds: [], timeSlot: 'evening', durationMin: 10, assignmentReason: '爸爸傍晚可用',
      status: 'pending', lockedByUser: false, riskLevel: 'low', version: 1 }} onComplete={onComplete} />);
    fireEvent.click(screen.getByRole('button', { name: '标记完成' }));
    expect(onComplete).toHaveBeenCalledTimes(1);
    expect(screen.getByRole('button', { name: '撤销' })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/components/TaskCard.test.tsx`  
Expected: FAIL，组件不存在。

- [ ] **Step 3: 实现方案草稿确认**

PlanPage 按时间排序任务，支持改派、改时间、锁定和重新生成；重生成时把锁定任务传给 `/api/plan`。存在 `assigneeId=null` 或规则冲突时，不允许确认为执行方案。

- [ ] **Step 4: 实现今日执行台**

TodayPage 显示总进度、成员进度和时间分组。TaskCard 支持查看详情、完成、5 秒撤销、跳过、改派和来源时间点。状态更新按 `task.id + version` 去重，已完成任务不可被后续方案覆盖。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/components/TaskCard.test.tsx`  
Expected: 完成、重复点击、撤销、跳过原因、锁定状态测试全部 PASS。

Run: `npm run build`  
Expected: 构建成功。

```powershell
git add app/src app/tests/components
git commit -m "feat: confirm and execute collaboration tasks"
```

---

### Task 9: 实现局部动态调整

**Files:**
- Modify: `app/server/prompts.ts`
- Create: `app/server/routes/adjust.ts`
- Create: `app/src/components/AdjustmentSheet.tsx`
- Create: `app/tests/fixtures/adjustment.json`
- Test: `app/tests/api/adjust.test.ts`
- Test: `app/tests/e2e/adjustment-flow.spec.ts`

- [ ] **Step 1: 写“爸爸加班”失败测试**

API 测试固定准备 3 项任务：1 项爸爸已完成、1 项爸爸待完成、1 项妈妈待完成。提交爸爸不可用后，断言调整结果只包含爸爸的待完成任务，已完成和妈妈任务完全不变。

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- --run tests/api/adjust.test.ts`  
Expected: FAIL，`/api/adjust` 不存在。

- [ ] **Step 3: 实现影响范围计算与调整 Prompt**

服务端先用确定性规则计算 `affectedTaskIds`，只把这些任务连同可用成员传给模型。Prompt 禁止修改 `completed`、`lockedByUser=true` 和未受影响任务；模型响应包含 `before`、`after`、`reason`。

- [ ] **Step 4: 实现前后对比与显式确认**

AdjustmentSheet 展示负责人、时间和原因的前后差异。点击“接受调整”才写入 Store；点击“保留原安排”关闭抽屉且不改数据。无可行分配时设置 `assigneeId=null` 并显示“待家庭安排”。

- [ ] **Step 5: 验证并提交**

Run: `npm test -- --run tests/api/adjust.test.ts`  
Expected: 局部调整、已完成保护、锁定保护、拒绝不落库、无解状态全部 PASS。

```powershell
git add app/server app/src app/tests
git commit -m "feat: adapt plans to family changes"
```

---

### Task 10: 完成安全、隐私、可访问性与演示降级

**Files:**
- Modify: `app/server/app.ts`
- Modify: `app/server/services/safety.ts`
- Modify: `app/src/styles/global.css`
- Modify: `app/src/components/AsyncState.tsx`
- Create: `app/src/data/preset-analysis.json`
- Test: `app/tests/api/analyze.test.ts`
- Test: `app/tests/e2e/preset-flow.spec.ts`

- [ ] **Step 1: 加入安全响应头和请求限制**

服务端设置 CSP、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`；JSON 请求体限制 1 MB；视频只通过 Multer 路由接收；日志只记录请求 ID、耗时、状态码和错误代码。

- [ ] **Step 2: 实现预置视频缓存降级**

`preset-analysis.json` 必须通过 `analysisSchema`。示例视频解析失败时，界面明确显示“实时解析暂不可用，已载入同一示例的最近一次合法结果”；用户上传的视频不得回退到预置结果。

- [ ] **Step 3: 完成可访问性检查**

保证点击区 ≥44×44 px、正文 ≥14 px、状态不只靠颜色、表单错误使用 `aria-describedby`、加载状态使用 `aria-live="polite"`、危险提示使用 `role="alert"`。

- [ ] **Step 4: 写端到端预置流程**

Playwright 使用 `Pixel 7` 设备配置，流程依次执行：打开首页 → 选择示例 → 确认步骤 → 快速创建家庭 → 生成方案 → 确认 → 完成一项任务。断言页面无未处理异常和横向溢出。

- [ ] **Step 5: 验证并提交**

Run: `npm run test:e2e -- tests/e2e/preset-flow.spec.ts`  
Expected: 核心预置流程 PASS。

```powershell
git add app
git commit -m "feat: harden safety and demo fallback"
```

---

### Task 11: 全量验证、远程部署与演示冻结

**Files:**
- Modify: `app/.env.example`
- Create: `app/README.md`
- Create: `docs/新生儿家庭协作台_演示手册.md`

- [ ] **Step 1: 固定环境变量契约**

`app/.env.example` 只包含名称和安全示例：

```dotenv
ARK_API_KEY=
ARK_MODEL=doubao-seed-2-0-lite-260428
PRESET_VIDEO_URL=https://example.invalid/preset-care.mp4
PUBLIC_BASE_URL=https://example.invalid
MEDIA_TTL_HOURS=24
PORT=4174
```

- [ ] **Step 2: 运行全量自动验证**

Run:

```powershell
npm test -- --run
npm run build
npm run test:e2e
npm audit --omit=dev
```

Expected: 单元/API/组件/E2E 全部 PASS；构建成功；运行时依赖无 high/critical 漏洞。

- [ ] **Step 3: 运行真实远程体验验收**

在非开发机手机网络中执行两次：

1. 预置视频全流程 ≤3 分钟。
2. 上传 30 秒至 2 分钟 MP4，确认真实模型返回结构化内容。
3. 将爸爸设为不可用，确认只有其未完成任务被调整。
4. 等待或手动触发媒体过期清理，确认旧 URL 返回 404。

- [ ] **Step 4: 写演示手册**

手册必须包含：60 秒项目叙事、3 分钟操作脚本、预置视频路径、备用网络方案、实时解析失败话术、API 额度检查、演示前 15 分钟检查表。

- [ ] **Step 5: 冻结版本并提交**

```powershell
git add app docs design
git commit -m "docs: finalize hackathon deployment and demo guide"
git tag hackathon-demo-v1
```

Expected: `git status --short` 无输出；标签指向通过全量验证的提交。

---

## 3. 40 小时时间盒

| 时间 | 里程碑 | 完成判定 |
|---:|---|---|
| 0–4h | Task 1–2 | Figma 主流程、工程可构建、Schema 测试通过 |
| 4–12h | Task 3–4 | 真实视频可上传，豆包返回合法 Analysis |
| 8–18h | Task 5–6 | 内容确认和家庭画像可操作 |
| 16–28h | Task 7–8 | 方案生成、确认与任务执行跑通 |
| 26–34h | Task 9 | 爸爸加班场景可局部调整 |
| 32–38h | Task 10 | 预置降级、安全与移动端 E2E 通过 |
| 38–40h | Task 11 | 远程部署、真机验收、演示冻结 |

硬停止点：第 28 小时仍未跑通方案确认时，立即取消 P1、所有动画和只读分享；第 34 小时仍未跑通动态调整时，只保留“成员不可用”这一种变化类型，但必须保持真实局部重排。

## 4. 最终验收矩阵

| PRD 要求 | 实施任务 | 自动验证 |
|---|---|---|
| 预置与真实视频双入口 | Task 3–5 | media/analyze API + picker 组件 + E2E |
| 结构化护理步骤与时间点 | Task 2、4、5 | Schema + model-json + 内容确认测试 |
| 家庭画像影响分配 | Task 6–7 | rules + plan API 测试 |
| 方案必须经用户确认 | Task 8 | PlanPage 组件行为 |
| 今日任务执行与进度 | Task 8 | TaskCard + E2E |
| 局部动态调整 | Task 9 | adjust API + adjustment E2E |
| 医疗内容不转执行任务 | Task 2、4、7、10 | Schema + safety + plan 测试 |
| 失败可恢复 | Task 3–5、10 | API 错误映射 + AsyncState + 降级 E2E |
| 24 小时媒体删除 | Task 3、11 | media-store 单测 + 远程验收 |
| 移动端与可访问性 | Task 1、5、10 | Pixel 7 E2E + 人工检查 |

## 5. 官方接口参考

- 火山方舟视频理解：https://docs.volcengine.com/docs/82379/1895586?lang=zh
- 火山方舟产品与 Responses API 入口：https://docs.volcengine.com/docs/82379/1795150?lang=zh
- Doubao-Seed-2.0-lite 全模态能力：https://developer.volcengine.com/articles/7636596381943070763

